{
  "name": "math-ranked",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "20.x",
    "npm": "10.x"
  },
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build:client": "npm install --prefix client && npm --prefix client run build",
    "build:server": "npm install --prefix server && npm --prefix server run build && (cd server && npx prisma generate)",
    "build": "npm run build:client && npm run build:server",
    "heroku-postbuild": "npm run build",
    "start": "node server/dist/index.js",
    "release": "node scripts/heroku-release.mjs",
    "db:push": "cd server && npx prisma db push --skip-generate --accept-data-loss && npx prisma generate",
    "seed": "cd server && npm run seed",
    "setup": "npm run install:all && npm run db:push && npm run seed && npm run build:client"
  }
}
